// Skeleton adapter for CJ Affiliate (Commission Junction). No API logic yet.
import { BaseProviderAdapter } from "../BaseProviderAdapter";

export class CJAdapter extends BaseProviderAdapter {
  readonly providerKey = "cj";
}
