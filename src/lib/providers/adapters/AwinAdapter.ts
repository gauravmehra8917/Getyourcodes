// Skeleton adapter for Awin. No API logic yet.
import { BaseProviderAdapter } from "../BaseProviderAdapter";

export class AwinAdapter extends BaseProviderAdapter {
  readonly providerKey = "awin";
}
