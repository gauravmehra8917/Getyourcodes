// Skeleton adapter for Impact (impact.com). No API logic yet.
import { BaseProviderAdapter } from "../BaseProviderAdapter";

export class ImpactAdapter extends BaseProviderAdapter {
  readonly providerKey = "impact";
}
