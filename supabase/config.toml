project_id = "bsnzezxeuttzfeummqdt"

[functions.affiliate-sync-preflight]
# The function verifies the Bearer JWT explicitly with Supabase Auth before
# checking the admin role. This supports modern opaque Edge secret keys.
verify_jwt = false

[functions.affiliate-sync-preview]
# The function verifies the Bearer JWT itself before the admin role lookup.
verify_jwt = false

[functions.affiliate-sync-preview-v2]
# The isolated V2 host verifies the Bearer JWT explicitly before privileged reads.
verify_jwt = false
